name: Cloud-Native Deployment Pipeline
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v3

      - name: Login to Container Registry
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/mardan-backend:latest \
            -t ${{ secrets.DOCKER_USERNAME }}/mardan-backend:${{ github.sha }} ./backend
          docker push ${{ secrets.DOCKER_USERNAME }}/mardan-backend:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/mardan-backend:${{ github.sha }}

      - name: Build & Push Frontend Docker Image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/mardan-frontend:latest \
            -t ${{ secrets.DOCKER_USERNAME }}/mardan-frontend:${{ github.sha }} ./frontend
          docker push ${{ secrets.DOCKER_USERNAME }}/mardan-frontend:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/mardan-frontend:${{ github.sha }}
